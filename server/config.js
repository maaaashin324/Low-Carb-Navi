module.exports = {
  // Redis setup
  db: {
    host: '127.0.0.1',
    port: '6379',
  },
  // server port setup
  express: {
    port: 3000,
  },
  // logger setup
  logger: {
    format: 'MMMM Do YYYY, h:mm:ss a',
  },
};
